name: trial
description: A new Flutter project.

publish_to: 'none' # Remove this line if you are publishing to pub.dev.

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.3
  hexcolor: ^3.0.1  # Ensure this version or newer is compatible
  flutter_screenutil: ^5.9.3 # Add this line


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assests/ellipse_1.jpeg # Fixed typo from 'assests' to 'assets'
